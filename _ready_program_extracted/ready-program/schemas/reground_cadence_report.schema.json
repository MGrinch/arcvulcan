{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "reground_cadence_report@1",
  "type": "object",
  "required": ["schema_version", "run_id", "issue_id", "overall", "every"],
  "properties": {
    "schema_version": {"const": "reground_cadence_report@1"},
    "run_id": {"type": "string"},
    "issue_id": {"type": "string"},
    "every": {"type": "integer"},
    "source": {"type": "string"},
    "overall": {"type": "string", "enum": ["PASS", "FAIL", "INCOMPLETE"]},
    "checks": {"type": "array"},
    "problems": {"type": "array"},
    "details": {"type": "string"}
  },
  "additionalProperties": true
}
