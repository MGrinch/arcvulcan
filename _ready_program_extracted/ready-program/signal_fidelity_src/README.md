# signal_fidelity_src

This is the optional **plugin bank** area.

Shipped in lean build:
- signal_fidelity_manifest.json
- signal_fidelity_filelist.txt

Not automatically extracted:
- the Signal Fidelity archive (RAR)

Extract locally into:
`signal_fidelity_src/XYZGL_SIGNAL_FIDELITY_002_ASSEMBLED/`
